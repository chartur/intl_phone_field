name: intl_phone_field
description: A customised Flutter TextFormField to input international phone number along with country code.
version: 3.3.1
homepage: https://github.com/vanshg395/intl_phone_field

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  arturs_common_utils:
    git: git@github.com:chartur/flutter_common_utils.git

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  test: ^1.17.12

flutter:
  assets:
    - assets/flags/
